name: Deploy to VPS via GHCR

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 拉取代码
      uses: actions/checkout@v4

    - name: 登录 GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: 构建并推送 Docker 镜像
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        # 镜像地址格式：ghcr.io/你的GitHub用户名/仓库名:latest
        tags: ghcr.io/${{ github.repository }}:latest

    - name: 部署到云服务器
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_PASSWORD }}
        script: |
          # 在服务器上登录 GHCR
          docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}
          # 拉取新镜像
          docker pull ghcr.io/${{ github.repository }}:latest
          # 清理并运行新容器
          docker stop test-cicd || true
          docker rm test-cicd || true
          docker run -d --name test-cicd -p 3000:3000 ghcr.io/${{ github.repository }}:latest