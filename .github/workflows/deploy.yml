name: Deploy to VPS via GHCR

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 拉取代码
      uses: actions/checkout@v4

    - name: 转换 GitHub 用户名为小写并保存为环境变量
      run: |
        OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
        echo "OWNER_LOWER=$OWNER_LOWER" >> $GITHUB_ENV

    - name: 登录 GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: 构建并推送 Docker 镜像
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ghcr.io/${{ env.OWNER_LOWER }}/test-cicd:latest

    - name: 部署到云服务器
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        password: ${{ secrets.VPS_PASSWORD }}
        script: |
          docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}
          docker pull ghcr.io/${{ env.OWNER_LOWER }}/test-cicd:latest
          docker stop test-cicd || true
          docker rm test-cicd || true
          docker run -d --name test-cicd -p 3000:3000 ghcr.io/${{ env.OWNER_LOWER }}/test-cicd:latest